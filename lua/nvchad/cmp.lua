-- This module provides custom completion (cmp) overrides
-- It's required by NvChad's cmp.lua configuration but was missing from the default setup
-- You can add your custom cmp settings here to override NvChad defaults

return {}
